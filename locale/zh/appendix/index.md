(chap_appendix)=
# Reference

主 path 经过 Part I-IV。Reference 保存你阅读时查阅的材料：

| 需求 | 位置 |
|------|-----|
| 查找 TIRx language feature | **{ref}`chap_language_reference`** |
| Compiler internals（lowering pipeline） | **{ref}`chap_arch`** |
| Debug asynchronous GEMM/FA 的 hang、crash、wrong result 或 slowdown | **{ref}`chap_warp_spec_debug`** |

完整的 `tvm.tirx` Python API 参见
[upstream TVM documentation](https://tvm.apache.org/docs/)。

TIRx native level（{ref}`chap_tirx_primer`）和 tensor layout model
（{ref}`chap_tirx_layout_api`）在 Part II 中覆盖。
